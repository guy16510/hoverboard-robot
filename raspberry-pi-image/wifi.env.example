# Copy to wifi.env before a local image build. wifi.env is gitignored.
# Do not commit the real password to this public repository.
TRASHCAN_WIFI_SSID='WiFi'
TRASHCAN_WIFI_PASSWORD='replace-with-local-password'
